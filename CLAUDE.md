# Advisor package

Read `AGENTS.md` for package maintenance and parent-agent coordination. Native Claude Code definitions are in `.claude/agents/`; their maintained sources are `agents/` plus `shared/working-rules.md`.
